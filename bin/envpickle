#!/usr/bin/env python

from PyROOT import environment
from optparse import OptionParser
import sys

parser = OptionParser()

options,args = parser.parse_args()

if len(args) != 1:
    sys.exit(1)

environment.define(args[0])
